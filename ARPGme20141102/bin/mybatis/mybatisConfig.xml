<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
	<properties resource="database.properties" />

	<typeAliases>
		<typeAlias alias="NpcEntity" type="com.arpg.gline.gameserver.module.npc.entity.NpcEntity" />
		<typeAlias alias="PlayerEntity" type="com.arpg.gline.gameserver.module.person.entity.PlayerEntity" /> 
	</typeAliases>
	
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${database.driver}" />
				<property name="url" value="${database.url}" />
				<property name="username" value="${database.user}" />
				<property name="password" value="${database.password}" />
			</dataSource>
		</environment>
	</environments>

	<mappers>
		<mapper resource="com/arpg/gline/gameserver/dao/map/npcMapper.xml" />
	    <mapper resource="com/arpg/gline/gameserver/dao/map/playerMapper.xml"/>
	</mappers>
</configuration>
