package netWork.resolve.receive;




/*
public class ListIntReceive extends ReceivableBuffer{

	public Object read(ByteBuffer byteBuffer) throws IOException
	{
		List<Integer> list = new ArrayList<Integer>();
		int length = readShort(byteBuffer);
		for(int i = 0; i < length; i++)
		{
			list.add(readInt(byteBuffer));
		}
		return list;
	}
}
*/